spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/blog?useUnicode=true&characterEncoding=utf-8
    username: root
    password: Crayfish980378
  jpa:
    hibernate:
      ddl-auto: update #確定好 schema 之後就改成 none
    show-sql: true

logging:
  level:
    root : info
    com.example: debug
  file:
    name: log/blog-dev.log
